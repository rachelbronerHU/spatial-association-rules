name: Release

# Pushing a version tag publishes that tag to PyPI. Nothing else does.
on:
  push:
    tags: ["v*"]
  workflow_dispatch:       # lets you run it by hand from the Actions tab, to check it works

jobs:
  publish:
    runs-on: ubuntu-latest
    environment: pypi
    permissions:          # naming any permission drops every default, so both are listed
      contents: read         # checkout needs this to read a private repo
      id-token: write        # how PyPI checks this really is our workflow. No token is stored.
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"

      - name: Build and check
        run: |
          pip install build twine
          python -m build
          twine check dist/*

      # Run from outside the checkout, or the source folder shadows the installed wheel
      # and the tests quietly check the wrong copy.
      - name: Run the tests against what was built
        run: |
          pip install dist/*.whl pytest
          cp -r tests "$RUNNER_TEMP/tests"
          cd "$RUNNER_TEMP"
          python -c "import spatial_association_rules as m; print('testing:', m.__file__)"
          pytest tests -q

      - uses: pypa/gh-action-pypi-publish@release/v1
